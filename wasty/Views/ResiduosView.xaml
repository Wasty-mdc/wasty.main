<UserControl x:Class="wasty.Views.ResiduosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:wasty.Views"
             mc:Ignorable="d"
             Height="Auto"
             Width="Auto"
             Background="#4CECEAE8" >
    
        <Grid Background="White" >
            <StackPanel>
                <TextBlock Text="Listado de Residuos"
                            FontSize="24"
                            FontWeight="Bold"
                            Margin="0,0,0,20" />

                <DataGrid ItemsSource="{Binding Residuos}"
                            SelectedItem="{Binding ResiduoSeleccionado}"
                            AutoGenerateColumns="False"
                            CanUserAddRows="False"
                            IsReadOnly="True"
                           >
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" />
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" />
                        <DataGridTextColumn Header="Denominación" Binding="{Binding Denominacion}" />
                        <DataGridTextColumn Header="LER" Binding="{Binding LER}" />
                        <DataGridTextColumn Header="Origen / Destino" Binding="{Binding OrigenDestino}" />
                        <DataGridTextColumn Header="Operador" Binding="{Binding OperadorTraslado}" />
                        <DataGridCheckBoxColumn Header="Activo" Binding="{Binding Activo}" />
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Grid>
</UserControl>
