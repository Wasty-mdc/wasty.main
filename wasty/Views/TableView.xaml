<UserControl 
      x:Class="wasty.Views.TableView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Height="Auto"
        Width="Auto"
        Background="Transparent"
      >

    <Grid>
        <DataGrid ItemsSource="{Binding Clientes}" 
                  AutoGenerateColumns="False" 
                  md:DataGridAssist.CellPadding="8"
                  md:DataGridAssist.ColumnHeaderPadding="10"
                  RowHeight="50"
                  CanUserSortColumns="True"
                  CanUserResizeColumns="True"
                  CanUserReorderColumns="True"
                  SelectionMode="Single"
                  Margin="20">

            <!-- Columnas -->
            <DataGrid.Columns>
                <!-- Foto de Perfil -->
                <DataGridTemplateColumn Header="Foto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding FotoPerfil}" Width="40" Height="40" Stretch="UniformToFill"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Nombre -->
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*" />

                <!-- Correo -->
                <DataGridTextColumn Header="Correo" Binding="{Binding Correo}" Width="2*" />

                <!-- Estado con etiqueta de color -->
                <DataGridTemplateColumn Header="Estado">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Estado, Converter={StaticResource EstadoToColorConverter}}" 
                                    CornerRadius="4" Padding="5">
                                <TextBlock Text="{Binding Estado}" Foreground="White" FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Botones de accion -->
                <DataGridTemplateColumn Header="Acciones" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Command="{Binding EditarCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                    <md:PackIcon Kind="Pencil" />
                                </Button>
                                <Button Command="{Binding EliminarCommand}" Style="{StaticResource MaterialDesignFloatingActionMiniButton}">
                                    <md:PackIcon Kind="Delete" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
